#include<stdio.h>
void main()
{
int b,h,area;
printf("enter the value of base:");
scanf("%d",&b);
printf("enter the value of height:");
scanf("%d",&h);
area=0.5*(b*h);
printf("Area=%d",area);
}